check process matchmaker matching "mono MatchMaker.exe"
  start program = "/bin/bash -c 'ls; cd <%= @deploy[:deploy_to] %>/current/matchmaker/; /usr/bin/mono MatchMaker.exe > <%= @deploy[:deploy_to] %>/current/log/matchmaker.log'"
  stop program  = "/usr/bin/pkill -f 'mono MatchMaker.exe'"
  
